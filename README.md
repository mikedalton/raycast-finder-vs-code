# raycast-finder-vs-code
Raycast extension to open a VS Code window based on the current finder window path

A simple Raycast for Mac extension to capture the path of the currently open Finder window, and open the folder in a new VS Code window. The extension makes the assumption that VS Code has been installed and has shell extensions configured to make the `code` command available.